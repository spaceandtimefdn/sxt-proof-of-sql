//! Tests for DynamicDoryCommitment.

#[cfg(test)]
mod dynamic_dory_commitment_test {
    use crate::proof_primitive::dory::DynamicDoryCommitment;

    #[test]
    fn test_dynamic_dory_commitment_type_exists() {
        let _: Option<DynamicDoryCommitment> = None;
    }
}
