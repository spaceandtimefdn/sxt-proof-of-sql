//! Tests for ColumnRef.

#[cfg(test)]
mod column_ref_test {
    use crate::base::database::{ColumnRef, ColumnType, TableRef};

    #[test]
    fn test_column_ref_type_exists() {
        let _: Option<ColumnRef> = None;
    }
}
