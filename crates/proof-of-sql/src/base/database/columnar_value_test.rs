//! Tests for ColumnarValue.

#[cfg(test)]
mod columnar_value_test {
    use crate::base::database::ColumnarValue;

    #[test]
    fn test_columnar_value_type_exists() {
        let _: Option<ColumnarValue> = None;
    }
}
