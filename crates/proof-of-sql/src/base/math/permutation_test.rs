//! Tests for permutation module.

#[cfg(test)]
mod permutation_test {
    use crate::base::math::permutation::Permutation;

    #[test]
    fn test_permutation_type_exists() {
        let _: Option<Permutation> = None;
    }
}
