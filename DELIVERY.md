# Improved Test Performance for proof-of-sql

## How to Run

1. Ensure you have Rust installed (https://rustup.rs/).
2. Run all tests with:

    make verify

This will run the test suite with Dory setup caching enabled, significantly reducing test runtime.
